[package]
name = "ort-server"
version = "0.1.8"
authors = ["Oliver Gould <ver@buoyant.io>"]
edition = "2018"
publish = false

[features]
# Generate code that is formatted.
rustfmt = ["tonic-build/rustfmt"]

[dependencies]
http = "0.2"
hyper = "0.13.3"
prost = "0.6"
prost-types = "0.6"
rand = { version = "0.7", features = ["small_rng"] }
serde = { version = "1", features = ["derive"] }
structopt = "0.3"
tokio = { version = "0.3", features = ["macros", "rt", "signal", "time"] }
tokio_02 = {version = "0.2.22", package = "tokio", features = ["time"] }
tokio-compat-02 = "0.1"
tonic = { version = "0.3", default-features = false, features = ["prost", "codegen", "transport"] }
tracing = "0.1"

[build-dependencies]
tonic-build = { version = "0.3", features = ["prost", "transport"], default-features = false }
